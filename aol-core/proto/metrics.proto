syntax = "proto3";

package metrics;

message MetricValue {
  string name = 1;
  double value = 2;
  map<string, string> labels = 3;
  int64 timestamp = 4;
}

message MetricsReport {
  repeated MetricValue metrics = 1;
}

